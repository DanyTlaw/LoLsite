<%= form_for([@champion, @counter]) do |f| %>
  <% if @counter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@counter.errors.count, "error") %> prohibited this counter from being saved:</h2>

      <ul>
      <% @counter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label "Champion Name" %><br>
    <%= f.text_field :champ_name %>
  </div>
 <div class="field">
  <%= f.label "Strong" %><br>
  <%= f.collection_radio_buttons :strong, [[true , 'Yes'] ,[false, 'No']], :first, :last %><br>
  <%= f.label "Weak" %><br>
  <%= f.collection_radio_buttons :weak, [[true, 'Yes'] ,[false, 'No']], :first, :last %>
  </div>
  <div class="field">
    <%= f.label "Champion Gegner" %><br>
    <%= f.text_field :champ_gegner %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
